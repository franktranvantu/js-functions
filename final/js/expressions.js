function getRandomNumber(upper) {
  const randomNumber = Math.floor(Math.random() * upper ) + 1;
  return randomNumber;
}

// const getRandomNumber = function(upper) {
//   const randomNumber = Math.floor(Math.random() * upper ) + 1;
//   return randomNumber;
// }

// Cannot call function expression before its declaration